<style>
/* General styles for the section */

  
.custom-five-section {
  background-color: #ffffff; /* Adds a background color to the entire section */
  margin: 0;
  padding-bottom: 30px;
}

.custom-five-section .section-title {
  text-align: center;
  font-family: BentonMod, sans-serif;
  font-size: 40px;
  line-height: 1.6;
  color: #000;
  margin-top: 50px; /* Adds space between the top of the section and the title */
  width: 80%; /* Adjusted for better readability */
  margin-left: auto; /* Centers the title block horizontally */
  margin-right: auto;
  margin-bottom: 10px;
  font-weight: normal; 
}

.custom-five-section .column p {
  font-size: 26px;
  line-height: 1.2;
}

  .section-title {
    padding-top: 50px;
  }


/* Styles for the columns container */
.four-columns-container {
    display: flex;
    justify-content: space-between; /* Adjusted for even spacing */
    text-align: center;
    padding: 0 40px; /* Add padding on both sides */
    margin-top: 0px; /* Decreased space between title and columns */
    margin-left: auto; /* Center the container */
    margin-right: auto; /* Center the container */
    width: 60%; /* Fixed width for regular mode */
}

/* Styles for each column */
.column {
    width: 23%; /* Adjusted for even distribution of columns */
    padding: 10px; /* Add padding inside each column for spacing */
  
}

.column h3 {
    font-family: BentonMod, sans-serif;
    margin-bottom: 5px; /* Space between the column title and text */
    font-size: 50px;
    margin-bottom: 0; /* Reduce space below each column title */
  font-weight: normal; 
  margin-top: 20px;
}

.column p {
    /* Text styles adjustments */
    font-family: BentonMod, sans-serif;
    font-size: 22px;
    line-height: 20px;
    text-align: center; /* Ensures text is centered */
}

  /* New styles for images */
.column img {
  max-width: 60%; /* Ensures the image doesn't exceed the column's width */
  height: auto; /* Maintains the aspect ratio of the image */
  max-height: 200px; /* Adjust this value as needed to control the image height */
  object-fit: cover; /* Ensures the image covers the set dimensions without distortion */
}


/* Responsive adjustments for mobile */
@media (max-width: 768px) {
  .four-columns-container {
    flex-direction: row;
    padding: 0 10px; /* Reduced padding for tighter layout */
    width: 100%; /* Adjust width to fit screen */
    gap: 5px; /* Reduce space between rows */
    padding-left: 130px;
    
    
  }

  .column {
    display: flex; /* Continues to use flexbox */
    align-items: center; /* Keeps items aligned */
    width: 100%; /* Ensures column takes full width */
    padding: 10px 0; /* Reduces vertical space */
    
  }

  .column h3 {
    margin: 10px;
    flex-basis: auto; /* Adjusts basis based on content size */
    flex-grow: 0; /* Prevents it from growing */
    padding-right: 10px; /* Space between title and paragraph */
  }

  .column p {
    flex-grow: 1; /* Allows paragraph to take up remaining space */
    text-align: left; /* Aligns paragraph text to the left */
    margin: 0 10 0 auto; /* Adjusts margin to push text to the right */
  }
}

.four-columns-container {
    width: 100%;
    max-width: 960px;
    padding: 0 20px;

}






</style>




<div class="custom-five-section">
<div class="custom-five-section">
  <h1 class="section-title">{{ section.settings.main_title }}</h1>
  
  <div class="four-columns-container">
    {% for block in section.blocks %}
      <div class="column">
        <img src="{{ block.settings.column_image | img_url: 'medium' }}" alt="{{ block.settings.column_title }}">

      </div>
    {% endfor %}
  </div>
</div>


{% schema %}
{
  "name": "Custom Five",
  "settings": [
    {
      "type": "text",
      "id": "main_title",
      "label": "Main Title",
      "default": "Default Main Title"
    }
  ],
  "blocks": [
    {
      "type": "text",
      "name": "Column",
      "settings": [
         {
          "type": "image_picker",
          "id": "column_image",
          "label": "Column Image",
          "info": "Choose an image for this column."
        },
      ]
    }
  ],
  "presets": [
    {
      "name": "Custom Five",
      "category": "Custom"
    }
  ],
  "max_blocks": 5
}
{% endschema %}
