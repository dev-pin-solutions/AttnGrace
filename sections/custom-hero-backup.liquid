<style>
.custom-hero-container {
  display: block;
  width: 100%;
  overflow: hidden; /* Hides any part of the image that exceeds the container's width */
  margin: 0;
  padding: 0;
  position: relative; /* Ensures positioning is relative to this container */
}

.custom-hero-image {
  width: 100%;
  height: auto; /* Scales the height automatically to maintain the aspect ratio */
  display: none; /* Hide images by default */
  z-index: -1; /* Ensure background images stay in the back */
}

.hero-content-container {
  position: absolute;
  top: 50px; /* Adjust based on your needs */
  left: 50%;
  transform: translateX(-50%);
  width: 100%;
  text-align: center;
  color: #fff;
  z-index: 1; /* Ensure content is above the background images */
}

.main-title, .subtitle, .image-column p {
  font-family: BentonMod, sans-serif;
  color: #000;
}

.main-title {
  font-size: 40px;
  line-height: 50px;
  margin: 20px auto 0;
  width: 80%;
}

.subtitle {
  font-size: 22px;
  line-height: 25px;
  margin: 0 auto 20px;
  width: 80%;
}

.subtitle {
  font-size: 22px; /* Adjust based on your design needs */
  line-height: 25px; /* Adjust for subtitle line-height */
  margin-bottom: 20px; /* Space after subtitle */
}

.image-columns-container {
  display: flex;
  justify-content: center; /* Centers the columns in the container */
  align-items: center; /* Align items vertically */
  flex-direction: row; /* Explicitly set to ensure horizontal layout */
  flex-wrap: nowrap; /* Prevents wrapping into a new line */
  margin: 20px auto; /* Center the container and add space above */
  width: auto; /* Adjust based on content size */
}

.image-column {
  flex: 0 1 auto; /* Do not grow, allow shrink, basis auto */
  padding: 10px; /* Provides space between the images */
  text-align: center; /* Ensures text is centered under the image */
}

.image-column img {
  width: 60px; /* Fixed width to keep images small */
  height: 60px; /* Fixed height to maintain aspect ratio */
  object-fit: cover; /* Ensures the images cover the area */
}

/* No changes needed for mobile, but ensure no conflicting styles for PC */



/* Show the desktop image on larger screens */
@media (min-width: 769px) {
  .desktop-hero-image {
    display: block;
  }
.hero-content-container {
    top: 10px; /* Decrease top margin for mobile */
    left: 0;
    transform: none;
    text-align: center;
    max-width: 50%; /* Adjust this value if you want a different constraint for larger screens */
  }

}

/* Show the mobile image on smaller screens */
@media (max-width: 768px) {
  .mobile-hero-image {
    display: block;
  }
  .hero-text-container {
    left: 0;
    transform: none;
    text-align: center; /* Center text within the container on smaller screens */
    max-width: 100%; /* Allows text container to use the entire width */
    width: 100%; /* Ensure the container spans the full width */
  }
  .main-title, .subtitle {
    width: 100%; /* Allows text to use the entire width of its container */
    margin-left: 0; /* Resets horizontal margins */
    margin-right: 0;
    text-align: center; /* Centers the text for mobile */
  }
}

</style>

<div class="custom-hero-container">
  <!-- New wrapping container -->
  <div class="hero-content-container">
    <div class="hero-text-container">
      <h1 class="main-title">{{ section.settings.main_title }}</h1>
      <p class="subtitle">{{ section.settings.subtitle }}</p>
    </div>

    <!-- Container for the 3 images -->
    <div class="image-columns-container">
      <div class="image-column">
        <img src="{{ section.settings.column_1_image | img_url: '360x' }}" alt="">
        <p>{{ section.settings.column_1_text }}</p>
      </div>
      <div class="image-column">
        <img src="{{ section.settings.column_2_image | img_url: '360x' }}" alt="">
        <p>{{ section.settings.column_2_text }}</p>
      </div>
      <div class="image-column">
        <img src="{{ section.settings.column_3_image | img_url: '360x' }}" alt="">
        <p>{{ section.settings.column_3_text }}</p>
      </div>
    </div>
  </div>

  <!-- Desktop image -->
  <img src="{{ section.settings.background_image_desktop | img_url: '1440x' }}" alt="Hero Image" class="custom-hero-image desktop-hero-image">

  <!-- Mobile image -->
  <img src="{{ section.settings.background_image_mobile | img_url: '375x' }}" alt="Hero Image Mobile" class="custom-hero-image mobile-hero-image">
</div>





{% schema %}
{
  "name": "Custom Hero",
  "settings": [
    {
      "type": "image_picker",
      "id": "background_image_desktop",
      "label": "Background Image (Desktop)",
      "info": "The background image used on desktop devices."
    },
    {
      "type": "image_picker",
      "id": "background_image_mobile",
      "label": "Background Image (Mobile)",
      "info": "The background image used on mobile devices."
    },
    {
      "type": "text",
      "id": "main_title",
      "label": "Main Title",
      "default": "Your Main Title Here"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Your subtitle here"
    },
    {
    "type": "image_picker",
    "id": "column_1_image",
    "label": "Column 1 Image"
    },
    {
      "type": "text",
      "id": "column_1_text",
      "label": "Column 1 Text",
      "default": "Your Column 1 Text Here"
    },
    {
  "type": "image_picker",
  "id": "column_2_image",
  "label": "Column 2 Image"
},
{
  "type": "text",
  "id": "column_2_text",
  "label": "Column 2 Text",
  "default": "Your Column 2 Text Here"
},
{
  "type": "image_picker",
  "id": "column_3_image",
  "label": "Column 3 Image"
},
{
  "type": "text",
  "id": "column_3_text",
  "label": "Column 3 Text",
  "default": "Your Column 3 Text Here"
}

    
    
  ],
  "presets": [
    {
      "name": "Custom Hero",
      "category": "Image"
    }
  ]
}
{% endschema %}
